# infrastructure/parser

Structure-aware document parser.

**Not implemented.** Per-format parsers live in `app/shared/utils/extractors/`. A unified parser (e.g. Unstructured.io) was planned but not adopted.
