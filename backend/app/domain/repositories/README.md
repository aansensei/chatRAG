# domain/repositories

Abstract repository interfaces (VectorRepo, QueueRepo, StorageRepo).

**Empty.** `application/retrieval` imports `infrastructure/vector/supabase` directly. Add interfaces here only when multiple backends need to coexist.
